plugins {
    id 'application'
    id 'maven-publish'
}

group = 'tech.hotu.bitstwinke.demo'
version = '1.0.0'

dependencies {
    implementation project(':lib')
}

application {
    mainClass = 'tech.hotu.bitstwinkle.demo.Demo'
}
